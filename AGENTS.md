# AGENTS.md

This project's agent/AI-assistant instructions live in [CLAUDE.md](CLAUDE.md) at the repo
root — that is the single source of truth (build steps, architecture, repo conventions,
gotchas). This file exists only so tools that specifically look for `AGENTS.md` by
convention still find guidance; keep this file a pointer, not a second copy, so the two
can't drift out of sync again.
